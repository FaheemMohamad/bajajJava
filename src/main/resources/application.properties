# Application Configuration
spring.application.name=bajaj-java-challenge

# Logging Configuration
logging.level.root=INFO
logging.level.com.bajaj=DEBUG

# API Configuration
api.base.url=https://bfhldevapigw.healthrx.co.in
api.generate.webhook=/hiring/generateWebhook/JAVA
api.submit.webhook=/hiring/testWebhook/JAVA

# User Details
user.name=Mohamad Faheem
user.regNo=22BCE9942
user.email=faheemmohamad87@gmail.com

# SQL Query (Replace with your actual SQL query)
sql.query=SELECT d.DEPARTMENT_NAME, ROUND(AVG(TIMESTAMPDIFF(YEAR, e.DOB, CURDATE())), 2) AS AVERAGE_AGE, GROUP_CONCAT(CONCAT(e.FIRST_NAME, ' ', e.LAST_NAME) ORDER BY e.EMP_ID SEPARATOR ', ') AS EMPLOYEE_LIST FROM EMPLOYEE e JOIN DEPARTMENT d ON e.DEPARTMENT = d.DEPARTMENT_ID JOIN PAYMENTS p ON p.EMP_ID = e.EMP_ID WHERE p.AMOUNT > 70000 GROUP BY d.DEPARTMENT_ID, d.DEPARTMENT_NAME ORDER BY d.DEPARTMENT_ID DESC;
